{
    "workflow": "cleanup",
    "comment": "cleanup code to be run after the etl",
    "type": "sql",

    "scripts": 
    [
        {"script": "etl/cleanup/note_nlp.sql",      "comment": ""}
    ]
}